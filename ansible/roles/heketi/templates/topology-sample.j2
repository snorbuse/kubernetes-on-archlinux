{
  "clusters": [
    {
      "nodes": [

{% for host in groups['worker'] %}
        {
          "node": {
            "hostnames": {
              "manage": [
                "{{ host }}"
              ],
              "storage": [
                "{{ hostvars[host].ansible_ssh_host }}"
              ]
            },
            "zone": 1
          },
          "devices": [
            "/dev/sdb"
          ]
        }
  {% if not loop.last %},{% endif %}
{% endfor %}

      ]
    }
  ]
}

